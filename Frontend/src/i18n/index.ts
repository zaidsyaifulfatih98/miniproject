import * as navbar from "./locales/navbar";
import * as footer from "./locales/footer";
import * as eventCard from "./locales/eventCard";
import * as eventGridSkeleton from "./locales/eventGridSkeleton";
import * as reviewForm from "./locales/reviewForm";
import * as reviewsList from "./locales/reviewsList";
import * as voucherInput from "./locales/voucherInput";
import * as upgradeOrganizerModal from "./locales/upgradeOrganizerModal";
import * as checkoutModal from "./locales/checkoutModal";
import * as mainLayout from "./locales/mainLayout";
import * as rootLayout from "./locales/rootLayout";
import * as home from "./locales/home";
import * as explore from "./locales/explore";
import * as detailEvent from "./locales/detailEvent";
import * as paymentPortal from "./locales/paymentPortal";
import * as becomeOrganizer from "./locales/becomeOrganizer";
import * as organizerProfile from "./locales/organizerProfile";
import * as login from "./locales/login";
import * as register from "./locales/register";
import * as forgotPassword from "./locales/forgotPassword";
import * as resetPassword from "./locales/resetPassword";
import * as profile from "./locales/profile";
import * as createEvent from "./locales/createEvent";
import * as dashboard from "./locales/dashboard";
import * as event from "./locales/event";
import * as ticket from "./locales/ticket";
import * as promos from "./locales/promos";
import * as transactions from "./locales/transactions";
import * as attendees from "./locales/attendees";
import * as report from "./locales/report";
import * as admin from "./locales/admin";
import * as common from "./locales/common";

export const translations = {
  id: {
    navbar: navbar.id,
    footer: footer.id,
    eventCard: eventCard.id,
    eventGridSkeleton: eventGridSkeleton.id,
    reviewForm: reviewForm.id,
    reviewsList: reviewsList.id,
    voucherInput: voucherInput.id,
    upgradeOrganizerModal: upgradeOrganizerModal.id,
    checkoutModal: checkoutModal.id,
    mainLayout: mainLayout.id,
    rootLayout: rootLayout.id,
    home: home.id,
    explore: explore.id,
    detailEvent: detailEvent.id,
    paymentPortal: paymentPortal.id,
    becomeOrganizer: becomeOrganizer.id,
    organizerProfile: organizerProfile.id,
    login: login.id,
    register: register.id,
    forgotPassword: forgotPassword.id,
    resetPassword: resetPassword.id,
    profile: profile.id,
    createEvent: createEvent.id,
    dashboard: dashboard.id,
    event: event.id,
    ticket: ticket.id,
    promos: promos.id,
    transactions: transactions.id,
    attendees: attendees.id,
    report: report.id,
    admin: admin.id,
    common: common.id,
  },
  en: {
    navbar: navbar.en,
    footer: footer.en,
    eventCard: eventCard.en,
    eventGridSkeleton: eventGridSkeleton.en,
    reviewForm: reviewForm.en,
    reviewsList: reviewsList.en,
    voucherInput: voucherInput.en,
    upgradeOrganizerModal: upgradeOrganizerModal.en,
    checkoutModal: checkoutModal.en,
    mainLayout: mainLayout.en,
    rootLayout: rootLayout.en,
    home: home.en,
    explore: explore.en,
    detailEvent: detailEvent.en,
    paymentPortal: paymentPortal.en,
    becomeOrganizer: becomeOrganizer.en,
    organizerProfile: organizerProfile.en,
    login: login.en,
    register: register.en,
    forgotPassword: forgotPassword.en,
    resetPassword: resetPassword.en,
    profile: profile.en,
    createEvent: createEvent.en,
    dashboard: dashboard.en,
    event: event.en,
    ticket: ticket.en,
    promos: promos.en,
    transactions: transactions.en,
    attendees: attendees.en,
    report: report.en,
    admin: admin.en,
    common: common.en,
  },
};
